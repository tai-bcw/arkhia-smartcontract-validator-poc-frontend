import { iso } from "./i18n";
import {
    favoriteList,
    hederaNetwork,
    nodeList
} from "./network";
import { colorPalette, darkMode } from "./theme";

const atom = {
    colorPalette,
    darkMode,
    iso,
    hederaNetwork,
    nodeList,
    favoriteList,
};

export default atom;
